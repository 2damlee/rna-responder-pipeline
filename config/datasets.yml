datasets:
  - accession: GSE78220
    source_type: geo
    expression_value_column: VALUE

    # to be confirmed after raw metadata inspection
    sample_id_column: geo_accession

    # candidate fields only; must be confirmed from phenotype_data
    response_label_fields: []
    timepoint_fields: []

    response_mapping: {}
    baseline_values: []